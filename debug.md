# Debug全流程追踪文档
## 基础信息
- 项目名称：CLink
- Debug启动时间：2026-03-22 00:21
- 关联模块/文件：`server/src`、`server/include`、`server/modules/process_inject`
- 环境信息：Windows 11；32GB 内存；存在 WSL2 Ubuntu 与完整 gcc/g++ 编译链；已安装 MinGW、JDK23、Go、Perl、PHP、CMake、Ninja；项目目标需同时支持 Windows 与 Linux 部署。

## 一、Bug核心信息
### 1.1 异常现象
当前任务不是单点复现 Bug，而是对服务端代码进行静态深度分析，主动挖掘潜在缺陷、稳定性风险、跨平台问题、并发问题、资源泄漏风险与可优化点。

### 1.2 预期功能
服务端应稳定运行在 Linux/Windows 环境，支撑客户端联机、会话管理、传输适配、心跳、监控、SOCKS 与进程相关能力，且在异常场景下保持可恢复、可观测、可维护。

### 1.3 影响范围
影响范围覆盖服务端主入口、配置加载、模块注册、网络收发、TLS/TCP 适配、会话管理、限流、可靠传输、日志与观测链路，以及 Windows 特有的进程注入模块。

## 二、进度追踪
- 当前进度：已完成服务端主链路、网络层、会话层、IPC、配置与安全边界的静态深度审查，并形成高优先级风险清单。
- 整体完成度：100%
- 待办事项：
  1. 按优先级确认先修复传输/可靠性主链路，还是先修复 IPC/并发控制问题。
  2. 为高风险点补充最小可复现验证用例或三进程联调方案。
  3. 修复后补做回归验证与性能/稳定性复核。

## 三、问题追踪记录
### 3.1 已解决问题
| 问题ID | 问题现象 | 根因分析 | 解决方案 | 修改文件 | 修改时间 | 验证结果 |
|--------|----------|----------|----------|----------|----------|----------|

### 3.2 未解决问题
| 问题ID | 问题现象 | 已尝试方案 | 当前卡点 | 需要用户提供的信息 | 计划解决时间 |
|--------|----------|------------|----------|--------------------|--------------|
| 001 | TLS 适配器接收路径没有做报文分帧，`async_read_some` 得到的是任意字节流分片，而上层直接按完整 `Packet` 反序列化，碎片包被直接丢弃 | 已审查 `tls_adapter.cpp` 与 `session_manager.cpp` 接收链路 | 需结合真实 TLS 流量验证分片/粘包比例 | 若要动态验证，需要允许搭建最小双端握手场景 | 2026-03-22 |
| 002 | 可靠传输接收链路没有处理 `Ack/Sack`，仅在收到 `Data` 时回 ACK，导致发送侧未确认队列无法正常清空 | 已全局搜索 `process_ack/process_sack` 调用点 | 需修复协议处理分支并验证重传/窗口行为 | 若要联调，需要明确优先验证 TCP 还是 TLS 场景 | 2026-03-22 |
| 003 | 可靠传输拥塞窗口判断使用 `unacked_packets_.size()`，把“未发送排队包”也计入飞行中窗口，可能导致排队包永久发不出去 | 已审查 `send_reliable()` 与定时器发送逻辑 | 需改为区分 in-flight 与 queued 状态 | 暂无 | 2026-03-22 |
| 004 | `Application` 的 IPC 命令处理、状态读取、连接/断开、重载配置跨线程并发执行，但内部大量字符串和对象无锁访问，存在数据竞争 | 已审查 `setup_ipc_handlers()`、`get_session_status()`、`connect_session()`、`on_session_event()` | 需统一控制面线程模型（strand/单线程调度） | 若需改造，建议先确认是否接受控制面串行化 | 2026-03-22 |
| 005 | Linux IPC 服务端为每个连接 `detach` 线程并捕获 `this`，服务停止后存在悬空访问风险；同时 `std::async + future.wait_for` 超时并不会真正取消任务 | 已审查 `ipc_linux.cpp` / `ipc_win.cpp` | 需改为受控工作线程或 Asio 驱动，并改造超时模型 | 暂无 | 2026-03-22 |
| 006 | ACL 语义与实现不一致：配置看起来像白名单客户端/IP，但实际按 `remote_endpoint` 的 `IP:port` 精确匹配，仅对 TLS 生效，且空名单默认放行 | 已审查 `acl.cpp`、`application.cpp`、`session_manager.cpp` | 需确认业务上到底按 IP、设备 ID、证书指纹还是账号控制 | 需要你确认 ACL 目标语义 | 2026-03-22 |
| 007 | 配置热重载会再次调用 `start_listen()`，但旧 listener 未被替换或停止，且返回错误未检查，导致重复绑定/重载失效 | 已审查 `reload_configuration()`、`apply_configuration()`、`start_listen()` | 需设计 listener 重建与无损切换策略 | 暂无 | 2026-03-22 |
| 008 | 对外返回的 JSON 大量通过字符串拼接生成，未统一转义，用户态字段可能产生非法 JSON 或注入问题 | 已审查 `get_session_status()` 与 IPC 返回体构造 | 需统一改为结构化 JSON 构造 | 暂无 | 2026-03-22 |
| 009 | 非 Windows 平台下 `DpapiHelper` 是 no-op，当前“自动加密 PSK 并回写配置”本质只是 Base64 包装，存在误导性安全风险 | 已审查 `application.cpp`、`dpapi_helper.hpp` | 需确认 Linux 端密钥保护策略 | 需要确认 Linux 侧可接受的依赖与安全级别 | 2026-03-22 |
| 010 | 连接状态建模不一致：会话在可靠引擎真正 ready 前就先写成 `Active`，`SessionStatus::Handshaking` 基本未使用，TLS 出站连接更可能出现“握手失败但会话已登记” | 已审查 `application.cpp`、`tls_adapter.cpp`、`session_manager.cpp` | 需重构状态机，明确 transport connected / tls handshaked / engine ready 的分层状态 | 暂无 | 2026-03-22 |

## 四、变更总览
### 4.1 所有修改文件清单
| 文件路径 | 修改内容概述 | 修改人 | 修改时间 |
|----------|--------------|--------|----------|
| `debug.md` | 初始化调试追踪文档，记录分析目标、环境与当前进度 | AI | 2026-03-22 00:21 |
| `require.md` | 初始化依赖与工具需求文档，当前记录为无新增依赖需求 | AI | 2026-03-22 00:21 |
| `debug.md` | 补充静态深度审查结论，记录高优先级潜在缺陷、并发风险与优化方向 | AI | 2026-03-22 00:44 |

### 4.2 新增/修改依赖清单
| 依赖名称 | 版本 | 用途 | 备注 |
|----------|------|------|------|
| 无 | 无 | 当前阶段仅进行静态分析，未引入新增依赖 | 如后续验证需要，再同步更新 `require.md` |

## 五、风险与待确认事项
1. 当前分析以静态代码审查为主，尚未结合实际运行日志、崩溃堆栈与三进程联调数据；但其中 `Ack/Sack 未处理`、`TLS 无分帧`、`IPC 并发模型不安全` 已属于高置信主链路问题。
2. ACL 的业务语义、Linux 侧密钥保护要求、以及是否接受控制面串行化改造，均需要用户确认。
3. 若后续进入修复阶段，建议优先级为：传输/可靠性协议链路 > IPC/线程模型 > 热重载与状态机一致性 > 安全与配置体验优化。
